# Core Networking & API
requests

# Web Framework
flask

# Math & Logic (Note: 'math' is built-in, so we don't list it here)

# Task Scheduling (for the 'On the Clock' logic)
celery

# Environment & Security
python-dotenv
